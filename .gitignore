# === Secrets & Credentials ===
.env
happypet-sheets-key.json
client_secret.json
*.json.bak

# === Python Cache ===
__pycache__/
*.pyc
*.pyo

# === Local Session / Dev Notes ===
.session-context.md
SESSION_STATE.md
FUTURE_REFERENCE.md

# === Pin Staging Queue (runtime, not source) ===
# _pin_queue/sent/ -- intentionally tracked for FB Queue dedup across GHA workspaces

# === Jekyll Build Output ===
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# === OS Junk ===
*:Zone.Identifier
.DS_Store
Thumbs.db

# === Logs (runtime, not source) ===
*.log
autopublish.log
LOGS/
setup_gh_secrets.sh
INFRA.md

# Local dev scripts (not for production)
scripts/

# Chewy validation reports (runtime artifacts, uploaded to GHA)
chewy_link_validation_*.json
